#Data Frame is to display the data in a table format. 
# Data frames can have multiple kinds of data inside it. It is more like creating tables of a HTML Document. The first set shall contain charecters that represent the data and rest will the data for that representation. 
Data_Frame = data.frame(
  People = c("Phaniraj", "Gopal", "Mahesh"),
  Salary = c("45000", "56000", "55000"),
  Age = c(47,51, 40)
)
# Data_Frame
# 
# Data_Frame[1]
# 
# Data_Frame["Salary"]

Data_Frame$Age
